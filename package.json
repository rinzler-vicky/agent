{
  "name": "agent-monorepo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.33.2",
  "scripts": {
    "lint": "pnpm -r --if-present run lint",
    "test": "pnpm run test:commitlint-config && pnpm -r --if-present run test",
    "test:commitlint-config": "node --test commitlint.config.spec.mjs",
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^20.5.2",
    "@commitlint/config-conventional": "^20.5.0",
    "husky": "^9.1.7"
  }
}
