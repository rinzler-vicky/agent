name: Copilot Setup Steps

on:
  workflow_dispatch:

jobs:
  copilot-setup-steps:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Print repository guidance
        run: |
          echo "Copilot/Codex/Claude must read AGENTS.md before working."
          test -f AGENTS.md
      # Customize the setup below for your stack.
      # Examples:
      # - uses: actions/setup-node@v4
      #   with:
      #     node-version: 22
      #     cache: pnpm
      # - run: corepack enable && pnpm install --frozen-lockfile
      # - uses: actions/setup-python@v5
      #   with:
      #     python-version: '3.12'
      # - run: pip install -r requirements-dev.txt
